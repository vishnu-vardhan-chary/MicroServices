spring:
  datasource:
    url: jdbc:mysql://localhost/emp
    username: root
    password: chary@121
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8083
  servlet:
    context-path: /employee
